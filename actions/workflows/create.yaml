---
version: '2.0'

bitovi_packs.create:
  input:
    - pack_name
    - github_exchange_org

  tasks:

    create_pack_repo:
      action: "github.create_pack_repo"
      input:
        pack: "{{ _.pack_name }}"
      publish:
        packs_status: "{{ task('create_pack_repo').result.result }}"